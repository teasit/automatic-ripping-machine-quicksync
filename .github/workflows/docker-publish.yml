name: Build and Publish Docker Images

on:
  push:
    branches:
      - main
    paths:
      - Dockerfile
      - Dockerfile.Ubuntu24
      - .github/workflows/docker-publish.yml
      - .gitmodules
      - scripts/**
      - libva
      - libva-utils
      - libvpl
      - vpl-gpu-rt
      - gmmlib
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          # jammy base, builds the QSV stack via do-release-upgrade + source builds.
          - dockerfile: Dockerfile
            image_suffix: ""
          # noble (Ubuntu 24.04) base, upgraded in place from the same jammy ARM image.
          - dockerfile: Dockerfile.Ubuntu24
            image_suffix: "-ubuntu24"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}${{ matrix.image_suffix }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=sha,prefix=sha-

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.dockerfile }}
          cache-to: type=gha,mode=max,scope=${{ matrix.dockerfile }}